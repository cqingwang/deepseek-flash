# Python 简化部署方案核对与修复计划

## 范围

- SSOT：`deploy.md`、当前 `config.yaml`，并以 `main` 分支 `scripts/*.sh` 与 `.env.dspark` 为行为基线。
- 目标：核对 `program.py` 的安装、双机启停、worker 守护、自检和环境生成逻辑；不实现模型下载。
- 坐标/空间变换：本任务无空间几何或坐标变换；模型路径、管理网地址和 RoCE 地址保持配置中的原值。

## 已确认根因

- 原始 worker 守护脚本用 `docker ps` 判断“运行中”；简化版复用了 `docker ps -a` 的存在性判断，停止容器会被误判为运行中，导致开机恢复不拉起 worker。
- head 缺失恢复路径需要区分“容器存在”与“容器正在运行”，远程 worker 检查必须保持原脚本的运行态语义。

## 修复路径

- 保留 `container_exists_*` 作为包含停止容器的存在性查询，新增运行态查询并在 `ensure` 与 head 恢复分支按原始脚本选择。
- 对命令行的等待秒数和验证 token 数做正/非负约束，避免无意义轮询或空 prompt 循环。
- 对 upstream start/stop 脚本检查可执行权限，避免 `isfile` 通过后运行阶段才抛出权限错误。
- 增加纯标准库单元回归，验证 stopped worker 会进入 compose 拉起路径，以及关键参数边界。

## 验证

- `python3 -m py_compile program.py`
- `bash -n deploy.sh`
- 针对性 Python 单元测试
- `gen-env` 模板键完整性与 `git diff --check`

## 模型注册布局核对（对照 main 分支修正：symlink 名单层 short，目标是宿主 <org>/<model>）

**main 分支最终底层效果（铁证）**：
- `scripts/.env.dspark` 模板：`DSPARK_MODEL_OFFICIAL=/cache/huggingface/models/<HF_MODEL_SHORT>`（**单层 short**），
  `DSPARK_ENCODING_FILE=.../<HF_MODEL_SHORT>/encoding/encoding_dsv4.py`；`HF_CACHE=${HOME}/.cache/huggingface` 挂载进容器；
- 模型落地 `$HOME/.cache/huggingface/models/DeepSeek-V4-Flash-0731/`（单层，无 org 前缀）；release `docs/07-deploy.md` 实测值同单层。

**宿主布局（用户确认）**：模型真实文件在 `/opt/models/<org>/<model>`（`/opt/models/deepseek-ai/DeepSeek-V4-Flash-0731`）。

**正确链路**：容器内 `/cache/huggingface/models/<short>` ← 宿主 symlink `model_links/<short>`（单层名）→ `/opt/models/<org>/<model>`。

**执行歪点（已回退）**：曾把 symlink 注册名、`DSPARK_MODEL_OFFICIAL`/`DSPARK_ENCODING_FILE`、`doctor_model` 检查路径
都改成 `<org>/<model>` 双层，偏离 main 模板的单层 `<HF_MODEL_SHORT>`；已回退单层，仅修正 docstring/日志语义
（symlink 目标为宿主 `<org>/<model>` 模型目录），`model_rel` 辅助已删除，`cmd_uninstall` 回退单层清理。

**回归测试**：`gen_env` 的 `DSPARK_MODEL_OFFICIAL` 为单层 short（对齐 main 模板）；`link_model` 注册名单层、
目标是完整 `<org>/<model>` 路径。

## 第二轮审计结果（README/main 锚点）

- 移除 `preflight` 兼容别名；唯一自检命令为 `doctor`。
- `cmd_start` 恢复到 main 的分支语义：不预先强制创建 worker；双缺时交给上游 start，只有 worker 已运行且 head 缺失时才直接 compose 拉起 head。
- `doctor` 在 install 前检查 `common.default_model` 真实目录，不依赖 install 才创建的模型 symlink。
- `install` 先解析并校验模型，再停止现有服务；`gen-env` 复用同一模型路径解析。
