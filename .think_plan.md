# Python 简化部署方案核对与修复计划

## 范围

- SSOT：`deploy.md`、当前 `config.yaml`，并以 `main` 分支 `scripts/*.sh` 与 `.env.dspark` 为行为基线。
- 目标：核对 `program.py` 的安装、双机启停、worker 守护、自检和环境生成逻辑；不实现模型下载。
- 坐标/空间变换：本任务无空间几何或坐标变换；模型路径、管理网地址和 RoCE 地址保持配置中的原值。

## 已确认根因

- 原始 worker 守护脚本用 `docker ps` 判断“运行中”；简化版复用了 `docker ps -a` 的存在性判断，停止容器会被误判为运行中，导致开机恢复不拉起 worker。
- head 缺失恢复路径需要区分“容器存在”与“容器正在运行”，远程 worker 检查必须保持原始脚本的运行态语义。

## 修复路径

- 保留 `container_exists_*` 作为包含停止容器的存在性查询，新增运行态查询并在 `ensure` 与 head 恢复分支按原始脚本选择。
- 对命令行的等待秒数和验证 token 数做正/非负约束，避免无意义轮询或空 prompt 循环。
- 对 upstream start/stop 脚本检查可执行权限，避免 `isfile` 通过后运行阶段才抛出权限错误。
- 增加纯标准库单元回归，验证 stopped worker 会进入 compose 拉起路径，以及关键参数边界。

## 模型注册布局核对（对照 main 分支修正：symlink 名名单层 short，目标是宿主 <org>/<model>）

**main 分支最终底层效果（铁证）**：
- `scripts/.env.dspark` 模板：`DSPARK_MODEL_OFFICIAL=/cache/huggingface/models/<HF_MODEL_SHORT>`（**单层 short**），
  `DSPARK_ENCODING_FILE=.../<HF_MODEL_SHORT>/encoding/encoding_dsv4.py`；`HF_CACHE=${HOME}/.cache/huggingface` 挂载进容器；
- 模型落地 `$HOME/.cache/huggingface/models/DeepSeek-V4-Flash-0731/`（单层，无 org 前缀）；release `docs/07-deploy.md` 实测值同单层。

**宿主布局（用户确认）**：模型真实文件在 `/opt/models/<org>/<model>`（`/opt/models/deepseek-ai/DeepSeek-V4-Flash-0731`）。

**正确链路**：容器内 `/cache/huggingface/models/<short>` ← 宿主 symlink `model_links/<short>`（单层名）→ `/opt/models/<org>/<model>`。

**执行歪点（已回退）**：曾把 symlink 注册名、`DSPARK_MODEL_OFFICIAL`/`DSPARK_ENCODING_FILE`、`doctor_model` 检查路径
都改成 `<org>/<model>` 双层，偏离 main 模板的单层 `<HF_MODEL_SHORT>`；已回退单层，仅修正 docstring/日志语义
（symlink 目标为宿主 `<org>/<model>` 路径），`model_rel` 辅助已删除。

**回归测试**：`gen_env` 的 `DSPARK_MODEL_OFFICIAL` 为单层 short（对齐 main 模板）；`link_model` 注册名单层、目标是完整 `<org>/<model>` 路径。

## 第二轮审计结果（README/main 锚点）

- 移除 `preflight` 兼容别名；唯一自检命令为 `doctor`。
- `cmd_start` 恢复到 main 的分支语义：不预先强制创建 worker；双缺时交给上游 start，只有 worker 已运行且 head 缺失时才直接 compose 拉起 head。
- `doctor` 在 install 前检查 `common.default_model` 真实目录，不依赖 install 才创建的模型 symlink。
- `install` 先解析并校验模型，再停止现有服务；`gen-env` 复用同一模型路径解析。

## 本次显示模式开关

- `deploy.sh -display off|on`（同时接受 `--display off|on`）映射到 `program.py display`。
- `off` 在 head/worker 设置 `multi-user.target`，`on` 设置 `graphical.target`。
- 只修改 systemd 默认启动 target，不执行 `isolate`，因此不影响当前 DSpark 服务；重启后生效。
- 增加 CLI、双机调用和 shell 入口验证，并同步 README。

## 本次部署参数审计与运行时日志

- 技术栈：Bash、Python、Docker Compose、双 DGX Spark；本任务不涉及空间坐标。
- SSOT：`config.yaml`、`dspark.env.json`；最终运行时以容器内实际进程 argv 和环境为准。
- 重点核对 `DEFAULT_THINKING=max` 与 README 性能基准 `low/off` 的差异，以及 `install` 不同步 `dspark/` runtime 导致的版本漂移。
- 启动成功后记录 head/worker runtime 文件 SHA-256、Compose 关键输入、容器配置和 `/proc/1/cmdline`；敏感值不打印。
- 验证：`bash -n deploy.sh dspark/start-deepseek-v4-flash-dspark.sh`、`python3 -m py_compile program.py`、`git diff --check`。

## 2026-08-19 纯净 Ubuntu 24.04 / 无 NVIDIA Sync 复刻要求

- 目标：在全新 Ubuntu 24.04 Server 上，以本项目文档、当前运行 Spark 的实际软件指纹和现有配置为 SSOT，整理根目录 `requirement.md`。
- 范围：只写硬件/系统/最小依赖/SSH 与 RoCE/NCCL 通信/模型与镜像/配置复制/部署验收要求；不新增 NVIDIA Sync 依赖，不修改部署实现。
- 版本策略：仓库当前 `dspark` 子模块为 `a6073994983851099c60042b5f69ec5df60d9205`；文档历史 pin 为 `a4ce87a...`，两者不一致，必须在可用 SSH 后采集运行节点的镜像 digest、容器 `/proc/1/cmdline`、CUDA/NCCL/vLLM 和 runtime commit，再冻结目标版本。
- 证据等级：静态文档/配置检查、双机宿主机通信检查、容器启动与 API 验收分开记录；SSH 采集失败时只记录为未验证，不把本地静态 pin 当作线上版本。
- 无 Sync 方案：head 通过 SSH/rsync 复制本项目、`dspark/` runtime、生成的 `.env.dspark` 和模型到 worker；使用 `ip/ethtool/rdma/ibdev2netdev/show_gids/mpirun/nccl-tests` 完成链路与 all-gather 检查。

## 2026-08-19 systemd head failed 修复

- 根因：上游 `start-deepseek-v4-flash-dspark.sh` 以 exit 3 表示 head 容器已存在；`program.py cmd_start` 捕获后无条件调用 `logtask(ERROR)`，而 `logtask(ERROR)` 立即 `sys.exit(1)`，导致 systemd 将“已有容器”误判为失败，后续 `return 0` 不可达。
- 修复：`cmd_start` 对 exit 3 执行 `wait_for_api` 二次确认；API 健康则返回 0，API 未就绪才保留失败语义。新增健康恢复和失败两条回归测试。
- 远端部署：仅更新两台 `/etc/dspark-vllm/program.py`，未重启 Docker/容器；两台与本地 SHA-256 均为 `5908bbd62a1c56a4a38c10f8bd90c2c1a6825876acfa9ff6de5d3e8f1f578af6`。
- 现场验证：head `dspark-vllm-head.service` 已为 `active (exited)`、`enabled`、ExecStart `status=0/SUCCESS`；容器保持运行，Bearer API 返回 HTTP 200。

## 本轮性能参数修复

- FlashInfer sparse MLA autotune 增加 2-request decode shape，并通过容器 hotfix 注入现有 0.1.1 镜像，覆盖日志中 k=5 对应的 12-token shape。
- Compose 层将 `max_cudagraph_capture_size` 显式限制为设备/镜像实际支持的 32，避免配置值 36 被 vLLM 运行时截断。
- SM12.1 不支持的 Symmetric Memory 路径显式关闭，保留已验证的 PYNCCL fallback；不强行启用未验证 backend。
- `max_num_scheduled_tokens=8168` 是 speculative decoding 对 8192 调度预算的内部预留，保持 1M/6/MTP5 基线，不用任意增大 batch 或降低 MTP 破坏 profile。
- 当前模型不支持 torch.compile；保持 CUDA Graph、DeepGEMM、FlashInfer custom fusion 路径，不使用 `--enforce-eager` 造成更大性能回退。

## API key 鉴权穿透

- `config.yaml` 使用 `common.api_key`，不再使用 `vllm_api_key`。

## 本轮模型切换与官方默认容量对齐

- 使用 Hugging Face 的 `drowzeys/keys-DeepSeekV4-Flash-GA-0731-Dspark-Abliterated-32-32` 本地快照。
- `common.model_variant=abliterated` 由 `program.py gen_env` 转换为 `ABLITERATED=1` 和 `DSPARK_MODEL_ABLITERATED`，不把官方 revision 误传给该模型。
- 对齐 main 分支官方默认容量：`MAX_MODEL_LEN=1048576`、`MAX_NUM_SEQS=6`、`MAX_NUM_BATCHED_TOKENS=8192`。
- 本地绝对路径加载时 `DSPARK_REVISION_ABLITERATED` 为空；下载 provenance 使用 abliterated 仓库自身 revision `6c47916f85e52b5e712223ca8f93952f90255714`。
- `gen_env` 写入 `VLLM_API_KEY`；Compose 将其注入容器，并在非空时向 `vllm serve` 追加 `--api-key`。
- 空字符串仍表示关闭鉴权；默认配置 `deepseek` 表示启用鉴权。
- 验证：`gen-env` 模板键完整性、Bash 语法检查、Compose 配置解析和 `git diff --check`。

## 本轮官方 c=4 容量 profile 对齐

- 官方历史容量实测使用 `MAX_NUM_SEQS=4`，因此本次比较固定并发 4，不把并发差异当作根因。
- 官方同一 profile 还使用 `MTP_NUM_TOKENS=6`、`MAX_NUM_BATCHED_TOKENS=16384`、`GPU_MEMORY_UTILIZATION=0.835`；当前实际容器曾落在 MTP5/8192/0.835。
- 将 MTP 深度与 batch budget 纳入 `config.yaml` SSOT，并由 `gen_env` 覆盖模板值，避免 install 后继续使用模板默认值。

## 本轮启动退出与热修复可靠性

- API key 开启后，启动健康检查和最小聊天请求统一通过 `api_curl` 注入 Bearer token；`/metrics` 保持无鉴权，避免监控采集被阻拦。
- Sparse MLA autotune hotfix 不再依赖容器源码中的完整单行文本，改为匹配 request-count 返回表达式中的 `min(max_num_seqs, 4)` 并补入 2-request shape。
- Sparse MLA autotune hotfix 在 head/worker 任一容器应用失败时立即让启动失败，不再用 `|| true` 掩盖性能修复未生效；其他已有兼容性 hotfix 保留原有非致命行为。

## 本轮 install 停止阶段配置修复

- 根因：`cmd_install` 在 `install_env` 之前调用 `cmd_stop`；当 runtime repo 中没有 `.env.dspark` 时，上游 stop 脚本无法读取 `WORKER_HOST`。
- 修复：`cmd_stop` 调用上游 stop 脚本时，从 `config.yaml` 注入 `worker.management_ip`，不改变新 `.env.dspark` 生成时序。
- 回归：新增测试验证 stop 子进程环境包含配置中的 `WORKER_HOST`；修复前测试失败，修复后完整测试通过。

## 本轮性能测试 CLI 语义改造

- 将公开入口 `chat_verify` 改为 `perf on|off [目标tokens]`。
- `on` 请求显式启用 thinking，`off` 请求显式关闭 thinking；默认目标 prompt tokens 保持 620000。
- 将内部函数改为 `cmd_perf`，同步更新 dispatch、help、README、deploy.md、docs 和回归测试。
- 输出使用明确的性能字段：thinking mode、目标/实际 prompt tokens、generated tokens、TTFT、prefill/decode 吞吐和 verdict。

## 本轮性能测试地址修复

- `common.api_url` 继续作为 head 本机 `live_check` 地址，避免改变部署机本地健康检查语义。
- 新增 `perf_api_url`，保留 `common.api_url` 的协议、端口和路径，把 host 替换为 `head.management_ip`。
- `perf` 的 tokenize/chat 请求和 JSON 报告均使用该管理网口地址；回归测试覆盖 Mac 远程执行时不得访问 `127.0.0.1`。

## 本轮 README 性能验收改造

- `perf` 不再用固定 3-token sentinel 判定 decode 路径，改为每组持续生成 128 tokens。
- 每次执行固定跑 8,299-token 短上下文基线，再跑命令参数指定的长上下文（默认 620,000）。
- 每组以 decode >= 60 tok/s 且生成 >= 128 tokens 为通过，总 verdict 要求两组均通过；TTFT/prefill 作为独立观测指标。

## 本轮 tokenizer 大请求断线修复

- 根因：旧构造器首次把约三分之一的长 prompt 一次性 POST 到 `/tokenize`，620k 目标会形成数 MB 请求体，服务端提前断开导致 Mac 侧 BrokenPipe/URLError。
- 修复：prompt 从小块开始、单次最多增长 32,768 tokens；tokenizer 临时断线最多指数退避重试 3 次。

## 本轮 fetch 模型下载

- 入口：`deploy.sh --fetch <org>/<model>`，由 `program.py` 统一分发。
- 模型存储根目录只读取 `config.yaml` 的 `common.model_lib`，目标固定为 `<model_lib>/<org>/<model>`。
- 下载复用 main 分支分块下载器逻辑：20 个 worker、8 MiB HTTP Range、`.chunks.json` 断点记录、失败重试和官方 LFS SHA-256 校验；默认使用 `hf-mirror.com`，`HF_ENDPOINT` 可切换源。
- 只在当前 head 下载，不自动启动/重启服务，也不自动同步 worker；下载完成后执行全量校验，worker 同步与复核仍按 06 章执行。
- 验证：CLI 参数回归、目标路径回归、`python3 -m unittest discover -s tests`、`py_compile`、`bash -n deploy.sh`、`git diff --check`。

## 本轮默认上下文与并发下调

- `deploy.sh --install` 通过 `dspark.env.json` 默认生成 `MAX_MODEL_LEN=600000`、`MAX_NUM_SEQS=4`。
- Compose 的参数回退值和启动日志同步为 600K/4，避免 `.env` 缺失时恢复旧的 1M/6 默认。
- 1M/更高并发仍可作为显式实验覆盖，不再作为安装默认；测试覆盖生产模板和实际 compose 参数。

## 本轮下载脚本代理兼容修复

- 根因：旧版清单阶段调用 huggingface_hub.HfApi，库内部读取 SOCKS 代理环境变量，在缺少 socksio 时于首次请求前失败；分块下载器自己的 httpx 客户端尚未执行。
- 修复：清单请求改用下载器已有 httpx 客户端，直接读取 HF tree API，支持分页、Bearer token 和清晰的 HTTP/JSON 错误；移除 HfApi 客户端依赖。
- 代理策略：默认不继承 HTTP(S)_PROXY/ALL_PROXY，适配 hf-mirror.com 直连；需要代理时显式设置 HF_FETCH_TRUST_ENV=1。
- 验证：新增分页清单回归测试；下载器测试、既有 48 个 Python 测试、py_compile 和 git diff --check 均通过。


# 本轮模型挂载路径修复

本轮在既有部署审计计划基础上，修复模型路径适配层：保留宿主 `/opt/models/<org>/<model>`
作为唯一模型根，Compose 增加只读 `/models` 精确映射，`gen_env` 生成 `/models/<org>/<model>`，
移除 install 阶段的 `model_links` symlink 创建。验证模型相关回归测试、Compose 静态挂载和
`git diff --check`。

# 本轮 install 重复启动修复

`cmd_install` 已先直接调用 `cmd_start`，再调用 `activate_units` 启动同一个 head
systemd unit，造成上游脚本并发/重复启动并返回 exit 22。修复为 install 只通过
systemd worker → head 顺序启动，保留 unit 的自恢复和 stop/restart 管理语义。

# 本轮 SparkDash API key 自动同步

`install` 读取 `common.api_key`，探测 `common.sparkdash_url/api/sparks`，按 head/worker
管理 IP 和 API 端口匹配注册项，调用 SparkDash 的加密 key API。SparkDash 不可用只告警，
不阻断 DSpark 部署；回归覆盖双 Spark 注入和 dashboard 不可用的非致命路径。

# 本轮 install oneshot 重启修复

`systemd` 单元为 `Type=oneshot` + `RemainAfterExit=yes`，对 `active (exited)` 单元执行
`start` 不会再次运行 `ExecStart`，导致 install 秒级返回且没有重新加载模型。改为
`systemctl restart`，保证每次 install 都重新执行 worker/head 部署入口。

该路径随后验证为不合适：`systemctl restart` 虽然会重新执行模型加载，但把上游启动
脚本的实时 stdout/stderr 收入 journal，导致 install 终端无输出且难以区分加载中与卡死。
最终方案恢复 `cmd_install -> cmd_start` 作为本次部署唯一启动入口；systemd 仅由
`deploy_units` enable，负责后续开机/守护，不在 install 末尾再次启动。

# 本轮 benchmark 官方参数对齐

将 `scripts/bench-miaai.py` 的官方 2026-08-14 请求覆盖参数移植到
`scripts/benchmark-0731.py`：默认 `thinking=false`、`max_tokens=128`、
`min_tokens=128`、`ignore_eos=true`；保留 `--natural` 进入旧的自然思考压测模式。
并发默认继续由 `common.max_request` 限制，避免发送超过当前服务容量的请求。
